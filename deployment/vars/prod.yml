---
# ============================================================================
# PRODUCTION ENVIRONMENT - Overrides for production only
# ============================================================================

# Domain (production has SSL)
domain_name: saul.run
enable_ssl: true

# Metrics secret (different per environment)
metrics_secret: "saul_m3tr1cs_s3cr3t_2024"

# Environment identifier
deploy_environment: production
