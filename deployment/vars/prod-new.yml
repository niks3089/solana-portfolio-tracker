---
# ============================================================================
# prod-new ENVIRONMENT - Overrides for 34.38.146.8
# (GCP VM `tokens-ci-runner` in europe-west1-b, project tokens-498908).
# Shared host: also runs a GHA self-hosted runner + tokens-* systemd timers,
# so we deliberately skip full apt upgrade and the host-level UFW firewall
# (GCP's VPC firewall is authoritative on this box).
# Replaces the retired Hetzner box at 213.239.141.25.
# ============================================================================

# Domain (Cloudflare DNS-only — traffic hits this box directly)
domain_name: portfolio.niks3089.com
enable_ssl: true
cloudflare_proxied: false

# Don't full-upgrade apt on a shared host; don't run host-level UFW (GCP firewall is auth.)
do_full_upgrade: false
enable_ufw: false

# Metrics secret (different per environment)
metrics_secret: "prod_new_m3tr1cs_s3cr3t"

# Environment identifier
deploy_environment: prod-new

# Helius API key (Enhanced Transactions API + RPC + websocket)
helius_api_key: "ac6750b1-daa3-45b6-b6f5-931bfb7b8999"
