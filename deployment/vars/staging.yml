---
# ============================================================================
# STAGING ENVIRONMENT - Overrides for staging only
# ============================================================================

# Domain (staging uses IP, no SSL)
domain_name: "45.76.155.10"
enable_ssl: false

# Metrics secret (different per environment)
metrics_secret: "staging_m3tr1cs_s3cr3t"

# Environment identifier
environment: staging
