export function History() {
    return (
        <section>
            <h2 className="text-2xl font-semibold">Trade History</h2>
            <p className="mt-2 text-text-secondary">
                Priced buys/sells from Helius. PR-3 will port the full sortable table from{' '}
                <code className="text-accent">/</code>.
            </p>
        </section>
    );
}
