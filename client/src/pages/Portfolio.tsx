export function Portfolio() {
    return (
        <section>
            <h2 className="text-2xl font-semibold">Portfolio</h2>
            <p className="mt-2 text-text-secondary">
                Multi-wallet aggregate view. The full dashboard lives at <code className="text-accent">/</code> for now;
                this React surface will replace it in PR-3.
            </p>
        </section>
    );
}
